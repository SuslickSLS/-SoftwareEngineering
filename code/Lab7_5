" ".join(list(map(str, fibonacci(10))))def fibonacci(n):
    fib_list = [0, 1]
    for i in range(2, n):
        fib_list.append(fib_list[i-1] + fib_list[i-2])
    return fib_list


file  = open("Lab7_5.txt", "w")

file.write(" ".join(list(map(str, fibonacci(10)))))
file.close()

fileRead = open("Lab7_5.txt", "r")
print(fileRead.read())
fileRead.close()
